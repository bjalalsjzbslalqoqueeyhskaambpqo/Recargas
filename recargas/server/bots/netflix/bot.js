const nombre = 'Netflix'
const disponible = false

async function procesar() {
  return { ok: false, mensaje: 'Servicio Netflix todavía no implementado.' }
}

module.exports = { nombre, disponible, procesar }
